{:title "进程管理"
 :template "docpage.html"
 :order 22}
---

Janet 支持启动、通信以及管理进程，主要通过两种方式。一种是通过 @code`os/execute` 提供的较简单的同步方法。另一种则是通过 @code`os/spawn` 和其他一些 @code`os/` 函数提供更细粒度控制的更复杂的方式。

先介绍 @code`os/execute` 函数，因为它更简单，同时也因为一旦理解了 @code`os/execute`，@code`os/spawn` 可能会更容易理解。
