(import ../../gen-docs :as gen-docs)
(import spork/test :export true)
(setdyn :source-linker (partial gen-docs/github-source-linker "janet-lang/spork" gen-docs/spork-version))
(setdyn :no-community-examples true)

{:title "test"
 :author "Calvin Rose"
 :license "MIT"
 :template "docpage.html"}
---

该模块包含一个简单的测试辅助工具，当你不需要专门的测试库时可以使用。

## 示例

### assert

`assert` 的修改版，带有更友好的错误处理。

@codeblock[janet]```
(test/assert false "How is this?")
# => ✘ How is this?
(test/assert true "OK")
# => ✔true
```


### assert-not

`assert` 的反向版本。

@codeblock[janet]```
(test/assert-not false "OK")
# => ✔true
```


### assert-error

若表达式抛出错误则测试通过。

@codeblock[janet]```
(test/assert-error "To err is natural" (error "Bad"))
# => ✔true
```


### assert-no-error

若表达式不抛出错误则测试通过。

@codeblock[janet]```
(test/assert-no-error "To not err is desired" (do "Good"))
# => ✔true
```


### start-suite

启动测试套件，统计所有测试和通过的测试数量。


### end-suite

结束测试套件，打印摘要，如果有失败则退出。


### 完整示例

简单测试套件的示例。

@codeblock[janet]```
(import spork/test)

(test/start-suite 0)

(test/assert true "is always true")
(test/assert-not false "is always false")
(test/assert-error "To err is natural" (error "Bad"))
(test/assert-no-error "To not err is desired" (do "Good"))

(test/end-suite)

# =>

Test suite 0 finished in 0.000 soconds
4 of 4 tests passed.
```


### timeit

使用 os/clock 计时代码执行，并打印结果。返回计时表达式的值。

@codeblock[janet]```
repl> (test/timeit (sum (seq [i :range [1 1000000]] (math/sqrt i))))
Elapsed time: 0.0718288 seconds
6.66666e+08
```


### capture-stdout

运行主体并捕获 stdout。返回元组，包含结果和捕获到的 stdout 字符串。

@codeblock[janet]```
(capture-stdout
  (print "Interesting output")
  true)
# => (true "Interesting output")
```


### capture-stderr

运行主体并捕获 stderr。返回元组，包含结果和捕获到的 stderr 字符串。

@codeblock[janet]```
(capture-stderr
  (print "Interesting output")
  true)
# => (true "Interesting output")
```


### supress-stdout

运行表达式，但抑制其 stdout。

@codeblock[janet]```
(suppress-stdout (print "Hello world!"))
# => nil
```


### supress-stderr

运行表达式，但抑制其 stderr。

@codeblock[janet]```
(suppress-stderr (eprint "Hello world!"))
# => nil
```

### assert-docs

当 `path` 为必填参数时，断言环境中所有公开绑定都有文档字符串。

@codeblock[janet]```
(assert-doc "/spork/test")
✘  suppress-stderr does not have proper doc
```

## 参考

@gen-docs/gen-prefix-current[test/]

