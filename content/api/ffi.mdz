(import ../gen-docs :as gen-docs)

{:title "FFI 模块"
 :order 0
 :nav-title "ffi"
 :template "docpage.html"}
---

Janet 的外部函数接口（FFI）模块用于与原生代码交互，无需编译原生"胶水"代码。但代价是 FFI 模块是"不安全的"——它无法保证甚至无法有效保护你的程序免于崩溃或触发异常行为。这与 Janet 通常的内存安全编程模型有所不同。

## 索引

@gen-docs/gen-prefix[ffi/]
